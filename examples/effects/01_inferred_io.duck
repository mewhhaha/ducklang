module (!init: Init) where

declare effect Io {
  read: () => Text
  print: (&Text) => Unit
}

declare Init {
  io: Io
}

let greet = () => do
  name <- Io.read()
  _ <- Io.print(&name)
  name
end;

result <- greet();
return { .result = result };
