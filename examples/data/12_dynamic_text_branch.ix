module (!init: Init) where

declare effect Input {
  flag: () => I32
}

declare Init { input: Input }

flag <- Input.flag()
let word = if flag {
  "forty"
} else {
  "six"
}

let result = len(word)
return { result }
