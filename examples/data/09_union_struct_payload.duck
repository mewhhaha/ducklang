const { .struct } = import "duck:prelude" ();

type User = struct { .id = Int, .score = Int }
type ScoreResult = | #Ok Int | #Err Text

let user: User = [1, 42];
let result: ScoreResult = #Ok (user.score);

if let #Ok score = result then
  score
else
  0
end;
