const alternate = 'w';

// The sigil distinguishes a captured constant from a new pattern binding.
let classify = (key: Char) => case key of
  'q' | #(alternate) => 40,
  _ => 0;

let computed = case 3 of
  #(1 + 2) => 2,
  _ => 0;

classify 'w' + computed
