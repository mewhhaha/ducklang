module (!init: Init) where

declare effect Input {
  index: () => I32
}

declare Init { input: Input }

const pair_type = struct { first: Int, second: Int }
let pair = pair_type { first: 20, second: 22 }

index <- Input.index()
let result: I32 = pair[index]
return { result }
