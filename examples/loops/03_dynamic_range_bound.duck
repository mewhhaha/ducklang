module (!init: Init) where

declare effect Input {
  bound: () => I32
}

declare Init { input: Input }

let total = 0;
bound <- Input.bound()

for value in 0..bound do
  total = total + value
end

let result = total;
return { .result = result };
