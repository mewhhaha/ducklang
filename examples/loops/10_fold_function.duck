let fold_range = (start, finish, initial, const step) => do
  let state = initial;

  for index in start..finish do
    state = step(state, index)
  end

  state
end;

const add_index = (total, index) => total + index;

fold_range(0, 7, 21, add_index)
