const { .get } = import "duck:prelude/runtime" ();

let message = "answer";
let answer = 0;

if (let 'a' = @cast(get(message, 0), Char)) {
  answer = 42
}

if true && !false {
  answer
} else {
  0
}
