const result_type = union {
  ok: Int,
  err: Text
}

let flag = 1
let result: result_type = if flag {
  result_type.ok(40)
} else {
  result_type.err("missing")
}

if let .ok(value) = result {
  value + 2
} else {
  0
}
