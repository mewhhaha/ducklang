module (!init: Init) where

declare effect Host {
  print: (bounded_borrow Text) => I32
}

declare Init { host: Host }

let message = scratch {
  let temporary: Text = append("hel", "lo")
  freeze temporary
}

result <- Host.print(borrow message)
return { result }
