module (!init: Init) where

declare effect Input {
  flag: () => Bool
}

declare Init { input: Input }

flag <- Input.flag()
let operation = if flag then
  (value: Int) => value + 1
else
  (value: Int) => value * 2
end;

let result: I32 = operation 21;
return { .result = result };
