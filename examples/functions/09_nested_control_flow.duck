module (!init: Init) where

declare effect Input {
  flag: () => Bool
}

declare Init { input: Input }

let value = 0;
outer <- Input.flag()

if outer then
  inner <- Input.flag()
  if inner then
    value = 42
  end;
else
  value = 21
end;

let result = value;
return { .result = result };
