type Maybe = | #Some I32 | #None

let select = (input: Maybe) => do
  let #Some value = input else do
    return 7;
  end;

  value + 1
end;

let present: Maybe = #Some 41;
let none: Maybe = #None;

select present + select none
