let gcd = rec (left, right) => do
  if right == 0 then
    left
  else
    if left > right then
      rec(left - right, right)
    else
      rec(right - left, left)
    end
  end;
end;

gcd(84, 30) * 7
