const { .identity } = import "duck:prelude/functional" ();

const apply_identity: (forall value.value -> value) -> I32 =
  (const identity) => if identity true then
    identity 41 + 1
  else
    0
  end;

comptime apply_identity identity
