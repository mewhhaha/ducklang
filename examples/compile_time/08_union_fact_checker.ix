const result_like = value_type => {
  let union { ok: Int, .. } = value_type
  value_type
}

const result_type: result_like = union {
  ok: Int,
  err: Text
}

let unwrap = (result: result_like) => {
  if let .ok(value) = result {
    value
  } else {
    0
  }
}

unwrap(result_type.ok(42))
