const readable = operations => do
  operations.read
  operations
end;

const scalar_operations = 0;
const scalar_operations = scalar_operations :+ {
  .read = value => value + 1
};

let read = (const operations: readable, value) => operations.read(value);
read(scalar_operations, 41)
