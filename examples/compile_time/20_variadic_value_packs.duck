const sum = (const ...values) => comptime do
  let total = 0;

  for value in values do
    total = total + value
  end

  total
end;

let first = case (2, 99) of
  (value, ...remaining) => value;

sum(19, 20, 1) + first
