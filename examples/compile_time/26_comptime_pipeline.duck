const pipeline = (const steps) => comptime do
  const [first, second, third] = steps;
  value => third(second(first(value)))
end;

const normalize = comptime pipeline [
  value => value + 1,
  value => value * 2,
  value => value - 6,
];

normalize 23
