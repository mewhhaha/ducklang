type NumberCalc =
  | .literal = Int
  | .add = Int
type TextCalc = .literal = Text

let eval_number = expression => {
  if let .literal(value) = expression {
    value
  } else {
    if let .add(left) = expression {
      left + 22
    } else {
      0
    }
  }
}

let expression: NumberCalc = NumberCalc.add(20)

eval_number(expression)
