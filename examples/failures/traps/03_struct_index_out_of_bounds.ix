module (!init: Init) where

declare effect Input {
  index: () => I32
}

declare Init { input: Input }

let pair = { first: 20, second: 22 }
index <- Input.index()
let result: I32 = pair[index]
return { result }
