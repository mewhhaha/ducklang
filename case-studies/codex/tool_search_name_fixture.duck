module () where

const { .tool_search_tool_name } = import "./tool_search_name.duck" ();

let score = 0;
let [namespace, local_name] = tool_search_tool_name();
if namespace == "" && local_name == "tool_search" then
  score = 1
end;

return { .score = score };
