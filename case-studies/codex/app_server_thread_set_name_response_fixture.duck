const { .materialize_thread_set_name_response } = import "./app_server_thread_controls.duck" ();

let score = 0;
let response = materialize_thread_set_name_response();
if let #Object fields = response then
  if let #Nil = fields then
    score = score + 1
  end;
end;

return { .score = score };
