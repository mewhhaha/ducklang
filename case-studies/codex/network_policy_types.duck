module () where

const { .struct } = import "duck:prelude/types" ();

type NetworkPolicyProtocol =
  | #PolicyHttp
  | #PolicyHttps
  | #PolicySocks5Tcp
  | #PolicySocks5Udp

type NetworkPolicyApprovalContext = struct {
  .host = Text,
  .protocol = NetworkPolicyProtocol,
}

return {};
